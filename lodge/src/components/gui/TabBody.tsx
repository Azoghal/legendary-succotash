import * as React from "react";

export default function TabBody(): React.JSX.Element {
    return (
        <div className="tab-body">
            and in here is going to go all thec content
        </div>
    );
}
